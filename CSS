body {
    margin: 0;
    font-family: 'Arial', sans-serif;
}

.navbar {
    margin-bottom: 0;
}

.page-heading {
    margin-top: 30px;
}

.main-section {
    background-color: #ecf0f1; /* Set background color to distinguish it from other content */
    padding: 20px;
}

/* Add any additional styling as needed */

